{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "运行项目",
      "type": "pwa-node",
      "request": "launch",
      // "runtimeExecutable": "nodemon",
      "args": [
        "${workspaceRoot}/node_modules/nodemon/bin/nodemon.js",
        // "NODE_PATH=\"${workspaceRoot}/src/index.ts\"",
        "${workspaceRoot}/.vscode/custom.ts" // 入口文件
      ],
      "sourceMaps": true,
      "cwd": "${workspaceRoot}",
      "protocol": "inspector"
    }
  ]
}
